
/**
 * Service file cleared to remove API key dependency and AI features.
 */
export const generateMemberSummary = async (): Promise<string> => {
  return "Fitur ringkasan AI dinonaktifkan.";
};

export const generateBio = async (): Promise<string> => {
  return "Fitur bio AI dinonaktifkan.";
};
